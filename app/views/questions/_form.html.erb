<%= form_for(@question, remote: true) do |f| %>
<p>
  <%= f.label :title %><br>
  <%= f.text_field :title %>
  </p>

<p>
  <%= f.label :text %><br>
  <%= f.text_area :text %>
</p>

<p>
  <%= f.submit (@question.persisted? ? "Update Question" : "Add Question") %>
</p>
<% end %>
